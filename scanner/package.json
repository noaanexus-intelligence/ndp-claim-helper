{
  "name": "ndp-claim-scanner",
  "version": "0.1.0",
  "private": true,
  "description": "On-premise scanner for HOSxP MySQL — finds records that will fail NDP validation, and safely fixes config/master tables only.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "mysql2": "^3.11.0"
  },
  "engines": {
    "node": ">=18"
  }
}
